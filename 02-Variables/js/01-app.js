// Inicializo una variable con un valor
var producto = "Monitor de 24 pulgadas";
console.log(producto);

// Las variables se pueden reasignar
producto = "Monitor de 19 pulgadas";
console.log(producto);

// JavaScript es un lenguaje dinamigo, no se especifica el tipo de dato
producto = 20;
console.log(producto);

// Se puede inicializar la variable sin un valor y asignarlo despues
var disponible;
disponible = true;

disponible = false;

// Inicializar multiples variables
var categoria = "Computadora",
  marca = "DELL",
  calificacion = 5;
