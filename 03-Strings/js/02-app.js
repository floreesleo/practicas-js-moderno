const producto = "Monitor de 20 pulgadas";

console.log(producto);

// Conocer la cantidad de letras de la cadena de texto
console.log(producto.length);

console.log(producto.indexOf("pulgadas")); // Te dice en que parte del string está la palabra deseada
console.log(producto.includes("Monitor")); // Te dice si la palabra está dentro del string
